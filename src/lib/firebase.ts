// Server-side (default) export: use server initialization in `server.ts`.
// Client-only APIs live in `firebase.client.ts` and must be imported from client code.

export { default, auth, database, storage } from './server';

